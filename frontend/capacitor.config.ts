import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sakshamgautam.weathergpt',
  appName: 'WeatherGPT',
  webDir: 'dist'
};

export default config;
